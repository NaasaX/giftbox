<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Giftbox{% endblock %}</title>
    <link rel="stylesheet" href="{{ assets_css }}/style.css">
</head>
<body>
    <div class="container">
        <header>
            {% block header %}
                <h1>Giftbox</h1>
            {% endblock %}
        </header>
        <nav>
            <ul>
                {% for item in nav_items %}
                    <li><a href="{{ item.url }}">{{ item.label }}</a></li>
                {% endfor %}
            </ul>
        </nav>
        <main>
            {% block content %}{% endblock %}
        </main>
        <div class="footer-container">
            <footer>
                {% block footer %}
                    <small>2025 Giftbox &copy; - IUT Nancy-Charlemagne</small>
                {% endblock %}
            </footer>
        </div>
    </div>
</body>
</html>

<style>
    .footer-container {
        position: absolute;
        bottom: 0;
        left: 50%;
        transform: translateX(-50%);
    }
    footer {
        background: #d9d6ff;
    }
</style>