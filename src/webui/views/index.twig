{% extends "layout.twig" %}

{% block title %}Accueil{% endblock %}

{% block content %}
<div class="container">
  <div class="text-content">
    <p class="intro-text">MyGiftBox.net est une application web qui permet de choisir, acheter 
                 et éventuellement offrir des coffrets-cadeaux construits sur mesure.</p>
                 <p>Un coffret cadeau est formé d'un ensemble de prestations : activités sportives,
                 activités culturelles, gastronomie, hébergement, visites, attentions particulières, etc...</p>
  </div>
  
  <div class="hexagon-scene">
    <div class="hexagon">
      <div class="face" style="transform: rotateY(0deg) translateZ(170px); background-image: url('/img/cate_attention.jpg');"></div>
      <div class="face" style="transform: rotateY(60deg) translateZ(170px); background-image: url('/img/cate_activite.jpg');"></div>
      <div class="face" style="transform: rotateY(120deg) translateZ(170px); background-image: url('/img/cate_hebergement.jpg');"></div>
      <div class="face" style="transform: rotateY(180deg) translateZ(170px); background-image: url('/img/cate_restaurant.jpg');"></div>
      <div class="face" style="transform: rotateY(240deg) translateZ(170px); background-image: url('/img/cate_petit_truc.jpg');"></div>
      <div class="face" style="transform: rotateY(300deg) translateZ(170px); background-image: url('/img/champagne.jpg');"></div>
    </div>
  </div>
</div>

<style>
body {
  margin: 0;
  background: linear-gradient(to bottom, #e0f7fa, #b2ebf2);
  overflow: hidden;
  display: flex;
  justify-content: flex-start; /* Alignement à gauche pour laisser de l'espace à la navbar */
}

.container {
  display: flex;
  flex-direction: column;
  align-items: center; /* Centrer tout le contenu horizontalement */
  justify-content: center; /* Centrer tout le contenu verticalement */
  height: 100vh;
  width: calc(100% - 250px); /* Laisser de l'espace pour la barre de navigation */
  padding: 20px;
  box-sizing: border-box;
  margin-left: 50px; /* Décalage vers la gauche */
}

.text-content {
  text-align: center;
  color: #333;
  font-family: Arial, sans-serif;
  margin-bottom: 30px;
}

.intro-text {
  font-size: 1.2em;
  margin-bottom: 40px;
  color: #555;
}

.hexagon-scene {
  height: calc(100vh - 150px); /* Ajuster la hauteur pour le texte */
  display: flex;
  justify-content: center;
  align-items: center;
  perspective: 1200px;
}

.hexagon {
  width: 200px;
  height: 200px;
  position: relative;
  transform-style: preserve-3d;
  transform: rotateX(-20deg) rotateY(0deg);
  animation: rotateHexagon 10s linear infinite;
}

.face {
  position: absolute;
  width: 200px;
  height: 200px;
  background-size: cover;
  background-position: center;
  border: 4px solid rgb(0, 0, 0);
  box-sizing: border-box;
  transform-origin: center;
}

@keyframes rotateHexagon {
  from {
    transform: rotateX(-20deg) rotateY(0deg);
  }
  to {
    transform: rotateX(-20deg) rotateY(360deg);
  }
}
</style>
{% endblock %}
