{% extends "layout.twig" %}

{% block title %}Bienvenue sur MyGiftBox{% endblock %}

{% block content %}
    <div class="home-container">
        <div class="welcome-card">
            <h1>Créez vos instants inoubliables</h1>
            <p>
                MyGiftBox.net vous permet de composer des coffrets-cadeaux sur mesure avec des prestations variées :
                sport, culture, gastronomie, hébergement, et bien plus encore.
            </p>
        </div>

        <div class="hexagon-zone">
            <div class="hexagon">
                <div class="face"
                     style="transform: rotateY(0deg) translateZ(170px); background-image: url('/img/cate_attention.jpg');"></div>
                <div class="face"
                     style="transform: rotateY(60deg) translateZ(170px); background-image: url('/img/cate_activite.jpg');"></div>
                <div class="face"
                     style="transform: rotateY(120deg) translateZ(170px); background-image: url('/img/cate_hebergement.jpg');"></div>
                <div class="face"
                     style="transform: rotateY(180deg) translateZ(170px); background-image: url('/img/cate_restaurant.jpg');"></div>
                <div class="face"
                     style="transform: rotateY(240deg) translateZ(170px); background-image: url('/img/cate_petit_truc.jpg');"></div>
                <div class="face"
                     style="transform: rotateY(300deg) translateZ(170px); background-image: url('/img/champagne.jpg');"></div>
            </div>
        </div>
    </div>

    <style>
        body {
            margin: 0;
            padding: 0;
            background: linear-gradient(145deg, #3f51b5, #9c27b0);
            overflow-x: hidden;
            color: white;
        }

        .home-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            box-sizing: border-box;
        }

        .welcome-card {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 40px;
            text-align: center;
            max-width: 700px;
            margin-bottom: 60px;
            box-shadow: 0 8px 24px rgba(0, 0, 0, 0.2);
        }

        .welcome-card h1 {
            font-size: 2.4em;
            margin-bottom: 20px;
            color: #ffffff;
        }

        .welcome-card p {
            font-size: 1.2em;
            color: #e0e0e0;
        }

        .hexagon-zone {
            perspective: 1200px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .hexagon {
            width: 200px;
            height: 200px;
            position: relative;
            transform-style: preserve-3d;
            animation: rotateHexagon 20s linear infinite;
        }

        .face {
            position: absolute;
            width: 190px;
            height: 200px;
            background-size: cover;
            background-position: center;
            border: 3px solid rgba(255, 255, 255, 0.4);
            border-radius: 10px;
            transform-origin: center;
            box-shadow: 0 0 12px rgba(255, 255, 255, 0.4);
        }

        @keyframes rotateHexagon {
            from {
                transform: rotateX(-20deg) rotateY(0deg);
            }
            to {
                transform: rotateX(-10deg) rotateY(360deg);
            }
        }

    </style>
{% endblock %}
